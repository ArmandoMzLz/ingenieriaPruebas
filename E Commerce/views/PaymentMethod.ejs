<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Registrar Método de Pago</title>
    <link rel="stylesheet" href="/css/car.css" /> 
    <link rel="icon" href="/favicon/logo.ico">
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            margin: 0;
        }
        main {
            flex: 1;
            padding: 40px 20px;
            max-width: 600px;
            margin: 0 auto;
        }
        h1 {
            text-align: center;
            margin-bottom: 30px;
            color: #222;
        }
        .payment-form {
            background-color: #fff;
            padding: 25px;
            border-radius: 16px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }
        .payment-form label {
            display: block;
            margin-top: 15px;
            font-weight: bold;
        }
        .payment-form input, .payment-form select {
            width: 100%;
            padding: 8px 10px;
            margin-top: 5px;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-size: 1em;
        }
        .payment-form button {
            margin-top: 20px;
            width: 100%;
            padding: 12px;
            background-color: #009688;
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 1.1em;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        .payment-form button:hover {
            background-color: #00796b;
        }
    </style>
</head>
<body>
    <%- include('partials/header')%>
    <main>
        <h1>Registrar Método de Pago</h1>
        <div class="payment-form">
            <form action="/Metodos-de-Pago" method="POST">

                <% if(error) { %>
                    <div class="errorMessage">
                        <%= error %>
                    </div>
                <% } %>
                <% if(exito) { %>
                    <div class="successMessage">
                        <%= exito %>
                    </div>
                <% } %>

                <label for="tipo">Tipo de tarjeta</label>
                <select id="tipoTarjeta" name="tipoTarjeta" required>
                    <option value="">Selecciona un tipo</option>
                    <option value="Visa">Visa</option>
                    <option value="MasterCard">MasterCard</option>
                    <option value="Amex">American Express</option>
                    <option value="Discover">Discover</option>
                </select>

                <label for="numero">Número de tarjeta</label>
                <input type="text" id="numeroTarjeta" name="numeroTarjeta" placeholder="Ej. 1234567890123456" pattern="\d{16}" maxlength="16" required />

                <label for="titular">Nombre del titular</label>
                <input type="text" id="titular" name="titular" placeholder="Ej. Juan Pérez" required />

                <label for="vencimiento">Fecha de vencimiento</label>
                <input type="month" id="vencimiento" name="vencimiento" required />

                <label for="cvv">CVV</label>
                <input type="text" id="cvv" name="cvv" placeholder="Ej. 123" pattern="\d{3}" maxlength="3" required />

                <button type="submit">Guardar Método de Pago</button>
            </form>
        </div>
    </main>

    <div id="footer-container"></div>
    <%- include('partials/footer')%>
</body>
</html>
