<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Pagina Principal</title>
    <link rel="stylesheet" href="/css/home.css" />
    <link rel="icon" href="/favicon/logo.ico">
</head>

<body>
    <%- include('partials/header')%>

    <form action="/Pagina-Principal" method="GET" class="search-form">
        <input type="text" name="q" placeholder="¿Qué producto buscas?" value="<%= typeof query !== 'undefined' ? query : '' %>">
        <button type="submit" aria-label="Buscar">
            <img type="image" src="/favicon/search.png" alt="Buscar"/>
        </button>
    </form>

    <div class="main-wrapper">
        <aside class="sidebar">
        <h3>Categorías</h3>
            <nav>
                <ul>
                    <li><a href="/Pagina-Principal?categoria=Procesador">Procesadores</a></li>
                    <li><a href="/Pagina-Principal?categoria=Tarjeta gráfica">Tarjetas Gráficas</a></li>
                    <li><a href="/Pagina-Principal?categoria=RAM">Memorias RAM</a></li>
                    <li><a href="/Pagina-Principal?categoria=HDD">HDD</a></li>
                    <li><a href="/Pagina-Principal?categoria=SSD">SDD</a></li>
                    <li><a href="/Pagina-Principal?categoria=Tarjeta madre">Tarjetas Madre</a></li>
                    <li><a href="/Pagina-Principal?categoria=Fuente de alimentación">Fuentes de Poder</a></li>
                    <li><a href="/Pagina-Principal?categoria=Gabinete">Gabinetes</a></li>
                    <li><a href="/Pagina-Principal?categoria=Refrigeracion">Refrigeración</a></li>
                    <li><a href="/Pagina-Principal">Todos los Productos</a></li>
                </ul>
            </nav>
        </aside>

        <main class="product-list">
            <h1>Componentes de PC destacados</h1>
            <div class="products-grid">
                <% if(productos.length===0) { %>
                    <p>No hay productos que mostrar</p>
                <% } else { %>
                    <% productos.forEach(producto => { %>
                        <a href="/Detalles/<%= producto.idProducto %>" style="text-decoration: none;">
                            <article class="product-card">
                                <img src="<%= producto.imagenProductoRuta %>" alt="<% producto.nombreProducto %>"/>
                                <h2><%= producto.nombreProducto %></h2>
                                <p><%= producto.descripcionProducto %></p>
                                <p class="price">$<%= producto.precioProducto %> MXN</p>
                                <form action="Carrito-de-Compras/agregarCarrito" method="POST">
                                    <input type="hidden" name="idProducto" value="<%= producto.idProducto %>">
                                    <input type="hidden" name="nombre" value="<%= producto.nombreProducto %>">
                                    <input type="hidden" name="precio" value="<%= producto.precioProducto %>">
                                    <input type="hidden" name="imagen" value="<%= producto.imagenProductoRuta %>">
                                    <input type="hidden" name="paginaOrigen" value="/Pagina-Principal<%= categoria ? '?categoria=' + categoria : (query ? '?q=' + query : '') %>">
                                    <button type="submit" class="btn-buy">Comprar</button>
                                </form>
                            </article>
                        </a>
                    <% }); %>
                <% } %>
            </div>

            <% if (added) { %>
                <div id="added-popup" class="popup">
                    Producto agregado al carrito
                </div>
            <% } %>
            <script>
                const popup = document.getElementById('added-popup');
                if (popup) {
                    popup.style.display = 'block';
                    setTimeout(() => popup.style.display = 'none', 2500);
                }
            </script>
        </main>
    </div>

    <%- include('partials/footer')%>
</body>
</html>